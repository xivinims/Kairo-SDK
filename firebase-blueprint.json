{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "User profile containing custom bots and personas",
      "type": "object",
      "properties": {
        "customBots": {
          "type": "array",
          "description": "List of user created AI bots"
        },
        "personas": {
          "type": "array",
          "description": "List of user created personas"
        },
        "updatedAt": {
          "type": "number",
          "description": "Timestamp of last update"
        }
      }
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "User profile document containing custom characters and personas"
    }
  }
}
